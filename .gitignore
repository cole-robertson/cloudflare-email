# Ruby
/.bundle/
/vendor/bundle
/pkg/
/tmp/
/coverage/
*.gem
.rbenv-version
.ruby-version
.DS_Store

# Lockfiles are not committed for libraries (consumers drive pinning)
/Gemfile.lock
/gemfiles/*.lock

# Node / Worker template
node_modules/
templates/worker/node_modules/
templates/worker/.wrangler/
templates/worker/package-lock.json

# Dogfood fixtures — not part of the gem
/trial/
/trial2/
