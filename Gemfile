source "https://rubygems.org"

gemspec

# SQLite is optional for consumers; development uses a patched release.
gem "sqlite3", ">= 2.9.6"

gem "mailbox-kit", path: File.expand_path("mailbox-kit", __dir__)
